# Note that this is mocked input that does not represent actual request
query samePlanet {
    allPeople {
        people {
            name
            homeworld {
                residentConnection {
                    residents {
                        name
                    }
                }
            }
        }
    }
}
